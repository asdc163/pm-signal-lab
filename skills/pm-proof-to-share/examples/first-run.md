# PM Proof to Share: first run

This is a fictional fixture. Paste the release note below into a compatible
Agent Skills client and ask it to use `pm-proof-to-share`.

## Fictional fixture

> Release note R-027, fictional package `pm-example-skill v0.3.0`: the package
> adds a first-run worksheet and a worked example. The fictional repository
> verifier passed, the local test suite reported 4 files and 10 tests, and a
> hosted check returned HTTP 200 with the expected English copy. The package has
> not been used by an external participant. The audience is a PM or product
> engineer who wants to inspect a small, source-linked workflow before adding a
> provider or login. The canonical release page is fictional:
> `https://example.invalid/pm-example-skill/releases/tag/v0.3.0`.

## Ask

Create the complete `pm-proof-to-share` output contract for one GitHub release
announcement. Keep `R-027`, `v0.3.0`, the proof layers, and the fictional
boundary visible. The first action must be to read or try the example, not to
star the repository.

## Expected review shape

The output should include one target reader, a claim-by-claim proof ledger,
one channel, a short English share draft, an exact try path, a feedback ask,
learning writeback, and a literal `## Not covered`. It must say that no
external participant session or adoption result exists.

## Not covered

This fictional fixture does not prove traffic, adoption, user satisfaction,
model quality, channel performance, or star growth. It contains no real quote,
customer data, credential, or permission to publish.
